Always read `quest.json` for the template and `state.json` for runtime state before answering. Persist any state change to `state.json` only (never modify `quest.json` for runtime changes). Verify the saved `state.json`, and then answer with only the current state block and possible actions block. This stable output is used by other agents.