- Design multi-agent flows from a plain-language requirement using only registered agents
- Create new flows (graph validated before saving: known agents, valid edges, acyclic)
- Inspect existing flows' nodes and edges
- Modify flows: rename, re-describe, restructure the node/edge graph
- Validate a stored flow or a proposed design without saving
- Delete flows (blocked while an instance is running)

What this agent does NOT do:
- Run flows (use the Orchestrator or the dashboard's Run button)
- Create or modify agents (use the Agent Creator)
- Modify source code or workspace files
