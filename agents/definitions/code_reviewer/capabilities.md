- Read and inspect any file in the workspace
- Search code for symbols, patterns, or anti-patterns
- Mark a task as `reviewing` while review is in progress
- Pass a task by setting status to `reviewed`
- Block a task with a structured reason listing every issue found

What this agent does NOT do:
- Implement fixes — issues are reported back, not corrected here
- Run tests or shell commands
- Modify source files
