- Generate clear, focused prompts on demand
- List currently registered agents to avoid name collisions
- Save the final prompt to `PROMPT.md` for downstream use

What this agent does NOT do:
- Generate prompts unsolicited — only when explicitly asked
- Run the prompt it produces
- Edit existing source files
