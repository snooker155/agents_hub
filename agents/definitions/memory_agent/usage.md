Use this agent for conversations that should persist across sessions in the shared memory pool.

Good fits:
- "Remember the user's preferences for next time"
- "What did we discuss about X last week?"

Poor fits:
- One-shot questions with no need for persistence — use the Basic Agent
- Tasks needing files, code, or tools beyond memory
