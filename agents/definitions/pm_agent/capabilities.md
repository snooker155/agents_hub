- Create top-level tasks for new projects or features
- Decompose tasks into subtasks with clear acceptance criteria
- Sequence subtasks via `create_sequence` to express dependencies
- Read and write project documents in the workspace

What this agent does NOT do:
- Pick which agent runs each subtask at execution time (the Orchestrator does that)
- Implement, test, or review work
