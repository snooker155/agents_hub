- Read, search, and list files inside the active workspace
- Modify code by writing whole files or applying unified diffs
- Create new source files and supporting artefacts
- Reason step-by-step using the `think` tool before and after each change
- Verify changes by re-reading affected files

What this agent does NOT do:
- Run shell commands or test suites (use the Developer Agent for that)
- Decompose tasks or assign other agents
- Touch files outside the provided workspace
