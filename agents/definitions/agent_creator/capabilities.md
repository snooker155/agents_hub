- List all registered agents and their tools
- Create new agents with a custom system prompt and tool set
- Inspect an individual agent's full configuration
- Modify existing agents' behavior and configuration
- Delete agents from the registry (system agents are protected)

What this agent does NOT do:
- Run agents (use the Orchestrator)
- Modify source code or workspace files
- Delete the system agents `orchestrator`, `decomposer`, or `agent_creator`
