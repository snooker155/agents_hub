// NOTE: the Documentation page's own copy. The source of truth for what
// this service does is the markdown corpus in /docs, which is also what
// agents search (tools/docs_tool.py). When the two disagree, /docs wins;
// this page is scheduled to render from it rather than restate it.
export default {
  documentation: 'Documentation',
  pageDescription: 'The in-app guide: core concepts, a tour of every surface, and short recipes for the first tasks. Pick a chapter from the menu on the left.',
  gettingStarted: 'Getting Started',
  coreConcepts: 'Core Concepts',
  featuresAtAGlance: 'Features at a glance',
  exploreTheSurfaces: 'Explore the surfaces',
  guidedWalkthroughs: 'Guided walkthroughs',
  shortEndToEndRecipes: 'Short, end-to-end recipes for the most common first tasks.',
  installRun: 'Install & run',
  ifTheBackendIsntOnline: 'If the backend isn\'t online yet, bring up the two services:',
  backendFastapi: '1. Backend (FastAPI)',
  frontendReactVite: '2. Frontend (React + Vite)',
  orWithDocker: 'Or with Docker',
  backendHttpLocalhost8000Frontend: 'Backend → http://localhost:8000 · Frontend → http://localhost:5173',
  httpLocalhost8000: 'http://localhost:8000',
  dockerDashboard8080: 'Dashboard → http://localhost:8080. nginx serves the built bundle and proxies /api to the backend, so the browser only ever talks to one origin.',
  httpLocalhost8410: 'http://localhost:8410',
  aHandfulOfObjectsMake: 'A handful of objects make up the whole system. Understanding how they nest is most of the learning curve.',
  workspaces: 'Workspaces',
  projects: 'Projects',
  tasks: 'Tasks',
  agents: 'Agents',
  memory: 'Memory',
  nodes: 'Nodes',
  sessions: 'Sessions',
  messages: 'Runs',
  models: 'Models',
  costs: 'Costs',
  tools: 'tools',
  views: 'Views',
  settings: 'Settings',
  sharedMemory: 'Shared Memory',
  nodesRuns: 'Nodes & runs',
  node: 'node',
  isALongRunningAgent: 'is a long-running agent process. A',
  run: 'run',
  session: 'session',
  flowsLoopsTeams: 'Flows, loops & teams',
  flow: 'flow',
  loop: 'loop',
  team: 'team',
  aBoundedRosterOfAgents: 'A bounded roster of agents that know each other and work over a shared message board.',
  keepReRunningAFlow: 'Keep re-running a flow until it',
  agentsHaveFiveComplementaryMemory: 'Agents have five complementary memory layers, bound to a pool (usually the workspace):',
  shared: 'Shared',
  notesStructuredSlotsAndA: '— notes, structured slots and a dated journal (',
  episodic: 'Episodic',
  discreteEventsInteractionTaskDecision: '— discrete events (interaction / task / decision / error / observation).',
  procedural: 'Procedural',
  reusableSkillsSurfacedByRelevance: '— reusable skills surfaced by relevance to the task.',
  knowledgeGraph: 'Knowledge graph',
  typedEntitiesAndLabelledRelations: '— typed entities and labelled relations.',
  rag: 'RAG',
  retrievalOverIndexedWorkspaceDocuments: '— retrieval over indexed workspace documents.',
  agentsReadWriteSharedMemory: 'Agents read/write shared memory with the',
  recall: 'recall',
  remember: 'remember',
  forget: 'forget',
  createSkill: 'Create skill',
  onlyNamesAndStatsAre: 'Only names and stats are injected into the prompt — values are fetched at runtime via tools.',
  chat: 'Chat',
  talkToAnyAgentDirectly: 'Talk to any agent directly — no need to start a node first. Supports attachments, flows and Telegram bridges.',
  orchestrator: 'Orchestrator',
  configureRoutingSoTasksAre: 'Configure routing so tasks are auto-assigned to the best-fit agent.',
  agentFlows: 'Agent Flows',
  buildMultiAgentGraphBased: 'Build multi-agent, graph-based workflows and run them with per-node streaming.',
  loops: 'Loops',
  reRunAFlowUntil: 'Re-run a flow until an agent judges the result good enough against a prose exit criterion.',
  teams: 'Teams',
  marketplace: 'Marketplace',
  browseAndCloneReadyMade: 'Browse and clone ready-made specialised agents and flows into your workspace.',
  plan: 'Plan',
  scheduleNotification: 'Schedule notifications, agent tasks and flow triggers; read what fired in the inbox.',
  evals: 'Evals',
  datasetsGradersAndModelSweeps: 'Datasets, graders and model sweeps — measure a prompt change instead of guessing.',
  playground: 'Playground',
  activationTitle: 'Synchronous or triggered',
  dropAgentsIntoASimulated: 'Drop agents into a simulated market or social world and watch them act, tick by tick.',
  viewsStudio: 'Views & Studio',
  chartsGraphs3dScenesAnd: 'Charts, graphs, 3D scenes and simulations agents render — then edit them by talking to them.',
  toolbox: 'Toolbox',
  skillsCatalog: 'Skills Catalog',
  webRequests: 'Web Requests',
  promptInjectionAudit: 'Prompt-injection audit',
  curateNotesSlotsEpisodesSkills: 'Curate notes, slots, episodes, skills and the knowledge graph for each workspace.',
  manageLongRunningAgentProcesses: 'Manage long-running agent processes and watch their live status.',
  curateTheModelCatalogIts: 'Curate the model catalog, its pricing, and the default per provider.',
  tokenAndUsdSpendBy: 'Token and USD spend by workspace, agent, model or project — with enforced budget caps.',
  editLayeredInstructionsSwapModels: 'Edit layered instructions, swap models, bind tools and memory, configure skills and delegation.',
  listRunningAndStoppedContainers: 'List running and stopped containers, read their logs, stop and remove them.',
  previewTheGeneratedDockerfileFor: 'Preview the generated Dockerfile for any agent before building.',
  buildTheSharedBaseImage: 'Build the shared base image once, then a per-agent image on top of it.',
  theContainersPage: 'The Containers page',
  executionModes: 'Execution modes',
  docker: 'docker',
  runYourFirstTask: 'Run your first task',
  createYourOwnAgent: 'Create your own agent',
  buildAndRunAFlow: 'Build and run a flow',
  getAChartInsteadOf: 'Get a chart instead of a wall of text',
  proveAPromptChangeActually: 'Prove a prompt change actually helped',
  putACapOnSpend: 'Put a cap on spend',
  tryItEndToEnd: 'Try it end to end',
  configureAProvider: 'Configure a provider',
  create: 'Create',
  install: 'Install',
  publish: 'Publish',
  discover: 'Discover',
  update: 'update',
  copy: 'copy',
  add: 'add',
  remove: 'remove',
  clear: 'clear',
  ops: 'ops',
  name: 'name',
  enabled: 'enabled',
  enableDisable: 'Enable / disable',
  default: 'default',
  none: 'none',
  this: 'this',
  which: 'which',
  why: 'why',
  and: 'and',
  not: 'not',
  before: 'before',
  page: 'page',
  outside: 'outside',
  bound: 'bound',
  launching: 'launching',
  terminal: 'terminal',
  work: 'work',
  localhost: 'localhost',
  api: 'API',
  git: 'Git',
  telegram: 'Telegram',
  connectors: 'connectors',
  streaming: 'Streaming',
  system: 'System',
  observability: 'Observability',
  credentials: 'credentials',
  apiKeys: 'API keys',
  customBackends: 'Custom Backends',
  localModels: 'Local Models',
  providers: 'Providers',
  pricing: 'Pricing',
  catalog: 'Catalog',
  catalog2: 'Catalog',
  usage: 'usage',
  usage2: 'Usage',
  run2: 'Run',
  llmJudge: 'LLM judge',
  graders: 'Graders',
  scenarios: 'Scenarios',
  modes: 'Modes',
  annotations: 'Annotations',
  controls: 'Controls',
  checkpoints: 'checkpoints',
  replay: 'Replay',
  studio: 'Studio',
  visualizer: 'Visualizer',
  view: 'view',
  registry: 'Registry',
  manifest: 'manifest',
  notification: 'Notification',
  agent: 'agent',
  agentTask: 'agent_task',
  privateKnowledge: 'private knowledge',
  summary: 'summary',
  total: 'total',
  daily: 'daily',
  monthly: 'monthly',
  weekly: 'weekly',
  hourly: 'hourly',
  done: 'done',
  env: '.env',
  exact: 'exact',
  substring: 'substring',
  regex: 'regex',
  assertions: 'assertions',
  jsonValid: 'json_valid',
  finalAgent: 'final_agent',
  createView: 'create_view',
  webSearch: 'Web search',
  centralized: 'centralized',
  autonomous: 'autonomous',
  parallel: 'parallel',
  aider: 'Aider',
  aiderMessage: 'aider --message',
  instructionsMd: 'instructions.md',
  capabilitiesMd: 'capabilities.md',
  usageMd: 'usage.md',
  modelsJson: 'models.json',
  serverPy: 'server.py',
  serverPyAbridged: 'server.py (abridged)',
  agentHubJsonRepositoryRoot: 'agent-hub.json (repository root)',
  dockerBuildFDockerfileAgenthub: 'docker build -f Dockerfile.agenthub -t aider-agenthub .',
  dockerCompose: 'docker compose',
  examplesImportedAgentsAiderAgenthub: 'examples/imported-agents/aider-agenthub',
  getApiHealth: 'GET /api/health',
  specNodesN1: 'spec.nodes.n1',
  taskAssignmentMode: 'TASK_ASSIGNMENT_MODE',
  eventsource: 'EventSource',
  pythonunbuffered1: 'PYTHONUNBUFFERED=1',
  liveUpdates: 'Live updates',
  aCostCeilingAndA: 'A cost ceiling and a wall clock.',
  aLeadReadsTheBoard: 'A lead reads the board each round and assigns the work. Orchestration with a fixed cast.',
  aListOfContainsNot: '— a list of contains / not_contains / matches / min_length rules in one grader.',
  aNoImprovementPatienceCounter: 'A no-improvement patience counter — stop when the score stops moving.',
  aSkillBelongsToThe: 'A skill belongs to the workspace that wrote it and is private to it by default.',
  agentPlayground: 'Agent Playground',
  agentsImportFromRepo: 'Agents → Import from Repo',
  agentsScheduleToo: 'Agents schedule too',
  agentsTalkFormOpinionsAnd: '— agents talk, form opinions and influence one another.',
  agentsTradeAgainstEachOther: '— agents trade against each other and the book moves.',
  andClick: 'and click',
  andPickTheFlowTo: 'and pick the flow to wrap.',
  andReadTheBreakdownBy: 'and read the breakdown by agent and model to see where the money actually goes.',
  andTurnItIntoAn: 'and turn it into an eval case.',
  anyOpenaiCompatibleEndpointWith: '— any OpenAI-compatible endpoint, with its own adapter, headers and default model.',
  apiHealth: '/api/health',
  apiStream: '/api/stream',
  apiSurface: 'API surface',
  attachFilesAndOptionallyStore: 'Attach files, and optionally store them into the workspace.',
  attachHubEntities: "Attach hub records too — a **task, view, project, scenario, loop, flow, team, agent or scheduled job**. The picked record is rendered into that message's context whether or not the agent has a tool that could have fetched it.",
  attachedVersusCatalogEntry: 'Attached versus catalog entry',
  boundsBecauseALoopCan: 'Bounds, because a loop can fail to converge',
  budgetsAreEnforcedNotJust: 'Budgets are enforced, not just displayed',
  catalogCostNumbersAreOnly: 'catalog — cost numbers are only as good as that table.',
  cheapAndDeterministic: '— cheap and deterministic.',
  checkRepository: 'Check repository',
  computeWhenApproximationIsNot: 'Compute — when approximation is not enough',
  curatesThatRawListDown: 'curates that raw list down to the models you actually want offered — only enabled models appear in the top-bar picker.',
  defaultPerProvider: 'Default per provider',
  defaultProvider: 'Default provider',
  eachConversationIsScopedTo: 'Each conversation is scoped to the workspace it was started in.',
  estimate: 'Estimate',
  fromRepositoryToARunning: 'From repository to a running imported agent',
  galleryOrPress: 'gallery, or press',
  giveItAClearInstruction: '. Give it a clear instruction.',
  gradersCarryWeightsSoOne: 'Graders carry weights, so one case can be scored on several axes at once.',
  hardLimit: 'hard limit',
  softLimit: 'soft limit',
  importFromRepo: 'Import from Repo',
  importingAnAgentFromIts: 'Importing an agent from its own repository',
  interactiveExampleBelowAScripted: 'Interactive example below — a scripted demo with sample data. Click a prompt to play it.',
  interactiveExampleBelowIllustrativeSample: 'Interactive example below — illustrative sample data, not your live instance.',
  isSetToAuto: 'is set to auto.',
  keysTheRuntimeReads: 'keys the runtime reads.',
  langfuseTracing: '— Langfuse tracing.',
  liveStreamingAgentExecutionMode: '— live streaming, agent execution mode, and task-assignment policy.',
  mMembersRRounds: 'M members × R rounds',
  nAgentsTTicks: 'N agents × T ticks',
  membersWorkTheRoundSide: '— members work the round side by side without waiting on each other.',
  minimumAndMaximumIterationsAnd: 'Minimum and maximum iterations, and a target score that ends it early.',
  nameAStateWorthReturning: 'name a state worth returning to.',
  needsSetup: 'needs setup',
  newFlow: 'New Flow',
  newLoop: 'New loop',
  newTask: 'New Task',
  noHubTools: 'No hub tools.',
  ofWhatItCommitsTo: 'of what it commits to doing on',
  ollamaAndLmStudioEndpoints: '— Ollama and LM Studio endpoints.',
  oneEnvelopeEverySurface: 'One envelope, every surface',
  onlyWhereARuleCannot: 'only where a rule cannot express it.',
  openaiAnthropicGoogleCredentialsAnd: '— OpenAI, Anthropic, Google credentials and base URLs.',
  orA: 'or a',
  orCloneOneFromThe: ', or clone one from the',
  otherThingsConfiguredInSettings: 'Other things configured in Settings',
  ownershipWorksLikeTheMarketplace: 'Ownership works like the Marketplace',
  pageSubstringOrJsonChecks: 'page — substring or JSON checks first, an',
  paletteOntoTheCanvasAgents: 'palette onto the canvas — agents, processors, conditions, transforms — and connect them to define the order of work.',
  persistsToTheSame: 'persists to the same',
  postStreamPathNdjsonOr: 'POST {stream_path} — NDJSON or SSE',
  putsItInTheGlobal: 'puts it in the global catalog, where every workspace can see it.',
  reCheck: 'Re-check',
  remindTheUserAtA: '— remind the user at a moment in the future.',
  scheduleTask: 'Schedule Task on Node',
  scheduledJobs: 'Scheduled jobs',
  socialWorld: 'Social World',
  stockExchange: 'Stock Exchange',
  startATaskWithA: '— start a task with a given agent when the time comes.',
  structuralChecksForToolShaped: '— structural checks for tool-shaped output.',
  studioEditingAViewBy: 'Studio — editing a view by talking to it',
  theAdapterInFull: 'The adapter, in full',
  theAgentDeclaresBecomeReal: 'the agent declares become real sliders and toggles, and their values persist per user.',
  theBackendIsOrganisedBy: 'The backend is organised by route domains, all served under',
  theBoard: 'The board',
  theExitCriterionIsProse: 'The exit criterion is prose',
  theHttpContract: 'The HTTP contract',
  theHubCannotSeeInside: 'The hub cannot see inside a run.',
  theOutliner: 'The outliner',
  theTickLogIsThe: 'The tick log is the artifact',
  theViewEnvelopeAbridged: 'the view envelope (abridged)',
  theViewRendersInlineIn: '— the view renders inline in the answer.',
  thenAssignItToA: ', then assign it to a task.',
  thenRunAndReadThe: ', then run, and read the score trajectory across iterations rather than only the last answer.',
  threeKindsOfJob: 'Three kinds of job',
  toKeepEditingItBy: 'to keep editing it by conversation.',
  toSeeWhatTheSweep: 'to see what the sweep will cost, then run it across the models you are choosing between.',
  tokenAndCostAccountingDepend: 'Token and cost accounting depend on the agent.',
  toolCallsAndRetrievedSkills: 'Tool calls and retrieved skills are shown inline as the agent works.',
  triggerAFlowWithAn: '— trigger a flow, with an optional JSON seed and a concurrency cap.',
  twoEndpointsAreWorthKnowing: 'Two endpoints are worth knowing by hand:',
  whatTheRepositoryMustProvide: 'What the repository must provide',
  whatYouGiveUp: 'What you give up',
  whereCasesComeFrom: 'Where cases come from',
  workedExampleAider: 'Worked example — Aider',
  troubleshootingFaq: 'Troubleshooting & FAQ',
  faq: {
    backendWontStart: {
      q: 'The backend won\'t start',
      a: 'Activate your virtualenv, install both requirements files (backend + agents), and make sure .env exists with a valid provider configuration.',
    },
    frontendCantReach: {
      q: 'The frontend can\'t reach the backend',
      a: 'The API client calls /api on its own origin, and the Vite dev server proxies that to the backend (VITE_API_PROXY, default http://localhost:8000). Confirm the backend is up at that address; point VITE_API_PROXY elsewhere if it listens on another one.',
    },
    runsFailImmediately: {
      q: 'Agent runs fail immediately',
      a: 'Check your provider credentials and selected model, that AGENT_EXECUTION_MODE matches your environment, and that the agent folder has a non-empty instructions.md.',
    },
    dockerCompose: {
      q: 'Docker compose starts but agent containers don\'t work',
      a: 'Usually it is not the wiring: compose already mounts /var/run/docker.sock into the backend and ships a Docker CLI there. Check the two things that are off by default instead: AGENT_EXECUTION_MODE=docker in .env (the default is local, and the backend has to be restarted afterwards), and the agent base image, which is built from the Containers page.',
    },
    budgetExceeded: {
      q: 'A run refused to start, saying the budget is exceeded',
      a: 'The workspace has a hard budget limit and the period\'s spend has reached it. Raise or clear the limit on the Costs page (0 = unlimited), or wait for the daily/monthly period to roll over. Hard limits are checked at launch, so nothing was half-run.',
    },
    zeroCost: {
      q: 'Costs show tokens but $0.00',
      a: 'Spend is computed by joining runs against catalog pricing, so a model with no price in the Models catalog contributes tokens and zero dollars. Set its input/output price per 1M tokens on the Models page — the numbers are retroactive, since they are derived at read time.',
    },
    modelMissing: {
      q: 'The model I want is not in the top-bar picker',
      a: 'The picker only lists models marked enabled in the Models catalog. Open Models → Catalog, run Discover for that provider (or add the model id manually), and enable it. Keys and base URLs stay in Settings; the model choice is only made here.',
    },
    textNotChart: {
      q: 'The agent answers in text where I expected a chart',
      a: 'Views come from the visualization tools. Check that create_view (and the kind-specific tools it needs) is bound to that agent on its Agents page — an agent only ever gets the tools you grant it.',
    },
    nothingStreams: {
      q: 'Nothing streams — output only appears when a run finishes',
      a: 'The dashboard streams over a single EventSource on /api/stream. If it is not connected the UI falls back to whatever it can poll. Check the connection indicator in the top bar, that live streaming is enabled in Settings → Agent execution, and that no proxy in front of the backend buffers server-sent events.',
    },
    jobNeverFired: {
      q: 'A scheduled job never fired',
      a: 'Jobs are fired by a background scheduler inside the backend, so nothing fires while the backend is down. GET /api/health reports whether the scheduler is alive; the Plan page shows each job\'s status (scheduled / paused / fired / cancelled / failed).',
    },
    reopenOnboarding: {
      q: 'How do I re-open this onboarding?',
      a: 'It\'s always here — visit Docs → Getting Started. The first-run popup only appears once (tracked in your browser\'s localStorage).',
    },
  },
  feature: {
    workspaces: {
      lead: 'Workspaces are the top-level boundary. Each one isolates its own tasks, projects, files and memory pools. Switch the active workspace from the top bar — most pages are scoped to it.',
      points0: 'Create one per client, product or experiment to keep context clean.',
      points1: 'The default workspace acts as an "all" view.',
      points2: 'Memory pools, sessions and tasks all inherit the workspace boundary.',
    },
    projects: {
      lead: 'Projects live inside a workspace and represent an actual deliverable or code repository — with repo settings, frontend/backend metadata and a group of related tasks.',
      points0: 'Attach a git repo to give agents a real codebase to work on.',
      points1: 'Group tasks under a project to track a feature end-to-end.',
    },
    tasks: {
      lead: 'A task is a unit of work with a status, an assigned agent, execution logs, an activity history and a result. Tasks can be decomposed into sub-tasks and run by an agent.',
      points0: 'Assign manually, or let the Orchestrator pick the best-fit agent.',
      points1: 'Open any task to watch its execution log and result stream live.',
      points2: 'Decompose large tasks into a checklist of sub-tasks.',
    },
    agents: {
      lead: 'Every agent is a folder of layered markdown — instructions.md (role), capabilities.md (what it can do) and usage.md (when to call it) — plus a registry entry holding the model, provider, tools and memory binding.',
      points0: 'The three layers are concatenated into the runtime system prompt.',
      points1: 'Model / provider / tools live in the registry, so editing prompts never touches JSON.',
      points2: 'Bind memory and skills to make an agent improve over time.',
    },
    marketplace: {
      lead: 'Browse ready-made specialised agents (and flows) and clone them into your workspace in one click, instead of authoring prompts from scratch.',
      points0: 'Clone a role like code reviewer, researcher or PM agent.',
      points1: 'Cloned agents are fully editable afterwards.',
    },
    flows: {
      lead: 'Flows are multi-step, graph-based agent workflows. Wire several roles together on a canvas and run them from the dashboard with streaming output per node.',
      points0: 'Each node runs a specific agent with its own prompt.',
      points1: 'Connect nodes to define the order and hand-off of work.',
      points2: 'Run a flow from Chat in flow mode and watch every node stream.',
      points3: 'The Registry page is the palette: agents, processors, conditions and transforms, dragged straight onto the canvas.',
    },
    toolbox: {
      lead: 'Tools are the concrete actions agents can take. Bind the right ones to each agent — an agent only ever gets the tools you grant it.',
      points0: 'Categories: filesystem, patching, shell, reasoning, memory, task management, agent coordination and management, flow management, scheduling, web, and visualization.',
      points1: 'Visualization is the largest family — the create_view / view_apply_ops / graph_* / scene_* / sim_* tools that build and edit views.',
      points2: 'The web tools (web_search, fetch_url) are recorded call-by-call under Web Requests.',
      points3: 'Tools are shared across agents; the binding is per agent.',
    },
    orchestrator: {
      lead: 'The Orchestrator decides which agent handles a task. Configure routing per workspace — turn auto-assignment on, choose how follow-ups are handled, and pick the execution mode.',
      points0: 'Assignment mode controls manual vs. automatic agent selection.',
      points1: 'Settings are stored per workspace.',
    },
    providers: {
      lead: 'Agents need at least one LLM provider. Credentials — OpenAI, Anthropic or Google keys, a custom OpenAI-compatible backend, or a local model served by Ollama / LM Studio — are configured in Settings; which model actually runs is decided in the Models catalog.',
      points0: 'Test connectivity from Settings: a green dot means the provider responded, red means check the key or base URL.',
      points1: 'The top-bar picker resolves in three tiers — workspace override, workspace default, global DEFAULT_PROVIDER.',
      points2: 'Only models enabled in the Models catalog appear in the picker.',
    },
  },
  open: 'Open',
  goTo: 'Go to',
  in: 'In',
  press: 'Press',
  onto: 'onto',
  andCreateOneOrUse: 'and create one (or use',
  thenSelectItInThe: '), then select it in the top bar.',
  assignAnAgentOrLet: 'Assign an agent — or let the Orchestrator pick if',
  runItAndOpenThe: 'Run it and open the task to watch the execution log, activity and final result stream in.',
  editTheThreePromptLayers: 'Edit the three prompt layers:',
  pickAModelProviderBind: 'Pick a model/provider, bind the tools it needs, and enable memory or skills if relevant.',
  testItFrom: 'Test it from',
  dragNodesFromThe: 'Drag nodes from the',
  configureEachNodesAgentAnd: 'Configure each node\'s agent and prompt, then save the flow.',
  runItFromChatIn: 'Run it from Chat in flow mode (or the flow page) and watch per-node output stream.',
  writeTheExitCriterion: 'Write the exit criterion as a sentence — "every claim carries a source" — and choose who judges it (by default, the flow\'s own final node).',
  setTheBoundsThatStop: 'Set the bounds that stop a loop which will not converge: max iterations, target score, patience, cost ceiling, wall clock.',
  bindTheVisualizationTools: 'Bind the visualization tools (starting with',
  toTheAgentOnIts: ') to the agent on its',
  askItSomethingWorthDrawing: 'Ask it something worth drawing in',
  openItFromThe: 'Open it from the',
  selectAnObjectInThe: ', select an object in the outliner and say what to change; undo, revert or save a checkpoint as you go.',
  findARunThatWent: 'Find a run that went wrong under',
  addGradersOnThe: 'Add graders on the',
  changeThePromptReRun: 'Change the prompt, re-run the sweep, and compare the score matrices.',
  priceTheModelsYouUse: 'Price the models you use in the',
  setASoftLimitTo: 'Set a soft limit to be warned and a hard limit to be stopped, over a daily, monthly or total period.',
  aRunThatWouldCross: 'A run that would cross the hard limit is refused at launch rather than truncated mid-flight.',
  runCapturesASingleExecution: 'captures a single execution (a task, a message, a chat turn) with its logs and metadata for later inspection.',
  threeWaysIntro: 'Three ways to put more than one agent on a problem. A',
  flowIsADag: 'is a DAG — each node runs once, in order. A',
  loopWrapsAFlow: 'wraps a flow and re-runs it, feeding back a reviewer\'s complaints, until an agent judges the result good enough. A',
  teamIsAFixedRoster: 'is a fixed roster of agents that know each other and talk over a shared board.',
  whenYoureReadyOpen: 'When you\'re ready, open the real',
  pageToTalkToYourOwn: 'page to talk to your own agents.',
  apiHealthExplains: 'is the one to read when something feels stuck: it reports the liveness of every background service — the plan scheduler, the run watchdog, the Telegram poller, the external-state publisher — rather than making you infer it from logs.',
  dashboardHoldsASingle: 'The dashboard holds a single',
  streamExplains: 'and everything that moves — chat tokens, flow nodes, team boards, loop iterations, simulation ticks, view frames — is a channel on it. If the top-bar connection indicator is dark, the backend is unreachable; nothing else needs polling to find out.',
  runsAgentsAsHostSubprocesses: 'runs agents as host subprocesses (default).',
  runsThemInManagedContainers: 'runs them in managed containers — see',
  executionContainers: 'Execution & Containers',
  executionModesContainers: 'Execution modes & containers',
  costsBudgets: 'Costs & budgets',
  planNotifications: 'Plan & notifications',
  importingAnAgentThatIsnt: 'Importing an agent that isn\'t ready',
  importNeverRefusesBefore: 'The import never refuses. A repository missing a manifest, a Dockerfile or an API key is still registered, marked',
  importNeverRefusesMiddle: ', carrying the list of what is missing. Its page has a service URL field and a',
  saveAndReCheck: 'Save & re-check',
  importNeverRefusesAfter: 'button, so the remaining work happens where the agent is rather than in a dialog you already closed.',
  skillAttachExplains: 'A skill sitting in your workspace does nothing on its own. Attaching it to an agent is what puts it in that agent\'s prompt, and attaching turns on that agent\'s skills setting — without it, the skill tools are never wired up and the skill would be silently inert.',
  learnedSkillsShowUp: 'Those show up here as "learned by an agent", ready to review and publish.',
  didTheToolRead: 'Did the tool read the page correctly?',
  extractionIsLossy: 'Extraction is lossy on purpose — HTML is stripped, JSON re-serialised, long pages truncated. When an answer is wrong, the log shows exactly what the model was handed.',
  wasTheResponseTrying: 'Was the response trying something?',
  eachCallIsScanned: 'Each call is scanned for injection phrasing, instructions hidden from human readers, attempts to close the untrusted-content envelope, credential-shaped strings and exfiltration-shaped requests.',
  outlinerExplains: 'lists the objects in the view; selecting one is injected into the next prompt as a scene-context note, so "make it bigger" resolves without re-reading the whole document.',
  undoRevert: 'Undo & revert',
  walkTheOpLogBackwards: 'walk the op log backwards;',
  annotationsExplain: 'let you mark up the view; export/print produces a static copy.',
  simulationViewsRunInBrowser: 'Simulation views run in the browser for real-time feel. When the numbers have to be right,',
  viewComputeExplains: 'runs the same scenario server-side on numpy and streams frames into the view — gravitational N-body, a 2D wave field, the 1D time-dependent Schrödinger equation, or the per-layer activations of a forward pass — optionally recording a replayable clip.',
  viewServeExplainsBefore: 'is the full-stack escape hatch: a generated backend on',
  viewServeExplainsProxy: 'exposed behind the view\'s scoped, origin-isolated proxy at',
  viewServeExplainsForward: 'It forwards only to loopback and never passes the dashboard\'s own auth through. Actually',
  viewServeExplainsOptIn: 'the child process is opt-in —',
  viewServeExplainsDefault: ', off by default, the same posture as the shell allowlist.',
  everyMemberActsEachRound: '— every member acts each round; the run ends when all of them declare themselves done.',
  exitCriterionExplainsBefore: '"Every claim carries a source" is not a predicate over state, it is an opinion — so it is written as a sentence and an agent scores the result against it after each pass. Normally that judge is the flow\'s own final node',
  exitCriterionExplainsMiddle: 'it can also be a named agent, or a bare model call. Iteration N+1 receives the rejected output',
  exitCriterionExplainsAfter: 'the specific complaints, so it revises instead of restarting.',
  llmJudgeExplains: '— a model grades the answer. The only grader that costs money, and the only one whose verdict is itself a model\'s opinion.',
  discoverExplains: 'asks each configured provider\'s live API what it offers, reusing the same probe as the connectivity test.',
  pricingExplains: 'is per model, in USD per 1M tokens — input, cached input and output; it is what every cost number in the app is computed from.',
  ragVectors: 'RAG & Vectors',
  ragVectorsExplains: '— Chroma, Pinecone or Qdrant, plus the embedding model (OpenAI, Google, sentence-transformers or Ollama).',
  settingsRagVectors: 'Settings → RAG & Vectors',
  instancesDoc: {
    title: 'Instances',
    lead: 'An instance is a live copy of an agent. A run is what a copy did and ends; an instance owns many runs, keeps its context, and can still be written to afterwards.',
    points0: 'One page for every copy running in the workspace — task runs, chat threads, flow nodes, team seats, nodes and containers alike.',
    points1: 'Standby means the carrier process is alive but idle; finished means the process is gone but the context is kept.',
    points2: 'Write to any copy from its page. A finished one is revived with its own history rebuilt from its journal; a node is handed the message and answers in its own process.',
    points3: 'Nodes and Containers still show the carriers. Instances shows what is actually running on them.',
    openPage: 'Open Instances',
    openHint: 'filter by agent, state or kind, or group a thousand identical copies under their agent.',
    tip: 'Every agent page has an Instances tab showing only that agent\'s copies.',
  },
  hooksDoc: {
    lead: 'Two ways to put a human, or the operator\'s own code, in front of a single tool call: hooks run around every call, and the approval gate holds a call until a person says yes. Both are off until you configure them; with neither configured, the only approval check is the advisory one some tools have always carried.',
    configTitle: 'Configuring hooks',
    configBody: 'Hooks are configured per workspace, in `<workspace>/.hooks.json` or under a `hooks` key in the workspace metadata. The metadata wins when both exist, so a centrally managed hook set is not overridden by a file inside a workspace an agent can write to.',
    matcher: '**matcher** is a regular expression matched against the whole tool id, so `run_shell|delete_file` means those two tools, not every tool whose name contains them. Leave it out (or use `*`) to match every tool.',
    type: '**type** is `command` (default) or `http`, with `timeout` in seconds (10 by default) and `fail_closed` deciding what happens when the hook itself breaks.',
    exempt: 'Hooks apply to action tools only. The reasoning scratchpad (`think`, `plan`, and friends) and `ask_user` are never wrapped, and a hook change applies to the next run, the same as any other agent setting.',
    payloadTitle: 'The payload',
    payloadBody: 'Every hook receives the same JSON object, on stdin for a command hook or as the body of a POST for an HTTP one: `hook`, `agent_id`, `run_id`, `task_id` (empty in chat), `workspace`, `tool` and `input`. `PostToolUse` adds an `output` field holding what the tool returned.',
    exitTitle: 'What a command hook\'s exit code means',
    exit0: '**0** allows the call.',
    exit2: '**2** denies it. The hook\'s stderr goes back to the agent as the tool\'s output, so the reason belongs there.',
    exitOther: '**Anything else** is logged and the call is allowed. Failing open is deliberate: a hook that cannot run is an infrastructure problem, not a reason to freeze every agent in the workspace. Set `fail_closed: true` on a hook whose failure should stop the call instead.',
    httpBody: 'An HTTP hook answers `{"decision": "allow" | "deny" | "ask", "reason": "..."}`. `ask` requires approval for this one call even when the tool is not on the approval list. A `PostToolUse` HTTP hook may also return `{"output": "..."}` to replace what the agent reads; a `PostToolUse` hook can never stop the call, because it already happened, so a deny there is only logged.',
    securityCallout: 'Hook processes run with the same scrubbed environment an agent\'s shell command gets: every `*_API_KEY`, `*_SECRET`, `*_TOKEN` and `*_PASSWORD` variable is removed before the hook starts. Treat the hook configuration as code: anyone who can write `.hooks.json` in a workspace can run commands as the agent.',
    gateTitle: 'The approval gate',
    gateBody: 'The gate holds a tool call until a person decides. It is opt-in per workspace:',
    gateSettingsLabel: 'workspace settings',
    gateTools: 'With it on, these tools need a yes every time: `run_shell`, `run_code`, `browser_act`, `delete_file`, `apply_unified_diff`, the `delete_*` entity tools, `remove_eval_case_tool`, `stop_run`, `stop_node`, `restart_node`, `stop_container` and `prune_run_logs`. An agent record can adjust the list for itself: `approval_tools` adds tool ids, `approval_exempt` removes them, and the exemption wins.',
    gateTask: 'In a task, the agent stops and the task moves to `awaiting_approval` with the pending call on it; **Approve** and **Deny** both re-run the agent with a resume instruction, and approving records the exact call (tool and arguments) so a repeat of it is let through once. In chat, there is no task to park, so the gate refuses the call and tells the agent to say exactly what it wanted to run: advisory, since the conversation carries the decision.',
    advisoryTip: '`tools/service_ops.py` and the entity run tools have always refused destructive calls with an `approval_required` error, letting the agent proceed once it calls again with `user_approved=True`. That convention is unchanged; the gate enforces the same shape of message rather than inventing a second one.',
  },
  nav: {
    groups: {
      startHere: 'Start here',
      workspace: 'Workspace',
      agents: 'Agents',
      automation: 'Automation',
      operations: 'Operations',
      guides: 'Guides',
    },
    gettingStarted: 'Getting Started',
    installation: 'Installation',
    concepts: 'Core Concepts',
    overview: 'Overview',
    chat: 'Chat',
    workspaces: 'Workspaces',
    projects: 'Projects',
    tasks: 'Tasks',
    plan: 'Plan & Notifications',
    views: 'Views & Studio',
    agents: 'Agents',
    importingAgents: 'Importing Agents',
    marketplace: 'Marketplace',
    skills: 'Skills Catalog',
    orchestrator: 'Orchestrator',
    teams: 'Teams',
    flows: 'Agent Flows',
    loops: 'Loops',
    tools: 'Toolbox',
    hooks: 'Hooks & Approval',
    memory: 'Shared Memory',
    webLogs: 'Web Requests',
    evals: 'Evals',
    playground: 'Playground',
    instances: 'Instances',
    nodes: 'Nodes & Sessions',
    containers: 'Execution & Containers',
    models: 'Models',
    costs: 'Costs & Budgets',
    providers: 'Providers',
    connectors: 'Connectors',
    tutorials: 'Walkthroughs',
    cli: 'CLI',
    cliApi: 'API & Stream',
    faq: 'Troubleshooting',
  },
  start: {
    lead: 'Agents Hub is a local multi-agent environment for planning, running and supervising AI-assisted work — agents, flows, loops, teams, scheduled jobs, evals, and the views agents draw. The checklist below inspects your running instance and ticks off each step as you complete it; start at the top.',
    firstRun: 'Your first five minutes',
    firstRun0: 'Configure one provider in [Settings](/settings) — an API key, or a local Ollama / LM Studio endpoint — then enable at least one model in [Models](/models). Only enabled models appear in the top-bar picker.',
    firstRun1: 'Pick or create a workspace in the top bar. Everything else — tasks, projects, memory, views, budget — is scoped to it.',
    firstRun2: 'Open [Chat](/chat), choose an agent (the built-in **Visualizer** is a good first target) and ask it something. Tool calls, retrieved skills and any view it draws render inline as it answers.',
    firstRun3: 'Watch what happened: the live copy that answered you is on [Instances](/instances), and the recorded run — prompt, tools, tokens, cost — on [Messages](/messages).',
    providersLead: 'Agents need at least one LLM provider. The work is split across two pages, and the split is worth learning once:',
    settingsHolds: '[Settings](/settings) holds **credentials** — API keys, base URLs, custom OpenAI-compatible backends, and the local Ollama / LM Studio endpoints.',
    modelsHolds: '[Models](/models) holds the **catalog** — which models are enabled, what each costs, and which one is the default per provider. Model choice lives here and nowhere else.',
    bothPagesWrite: 'Both pages write to the same `.env` / `models.json` the runtime reads, so hand-editing is optional. A minimal `.env` looks like:',
    pickerTip: 'The picker in the top bar lists every *enabled* catalog model and resolves in three tiers: an explicit workspace override, then the workspace default, then the global `DEFAULT_PROVIDER`. The dot next to each row is a live connectivity probe.',
    whereNext: 'Where to go next',
    whereNextLead: 'Once an agent has answered you, these are the surfaces that do the rest of the work. Each card opens the live page; the Docs sidebar carries a fuller page per feature.',
    nextInstances: 'Every live copy of an agent in one place — standby, running or finished — and you can write to any of them.',
    nextViews: 'Charts, graphs, 3D scenes and simulations agents draw instead of prose — then edit them by talking to them in Studio.',
    nextPlan: 'Schedule notifications, agent tasks and flow triggers. Agents schedule their own too, and results land in the inbox.',
    nextTeams: 'A small, fixed roster of agents that know each other and work over a shared message board.',
    nextLoops: 'Re-run a flow, feeding back a reviewer\'s complaints, until an agent judges the result good enough.',
    nextEvals: 'Turn "did that prompt change help?" into a score matrix across models — plus a prompt-injection audit.',
    nextSkills: 'Reusable procedures agents retrieve by relevance — and write for themselves at runtime.',
    nextImported: 'Bring in an agent that already exists in its own repository, with its own dependencies, over HTTP.',
    nextCosts: 'Token and dollar spend by workspace, agent, model or project — with budget caps enforced at launch.',
    nextConnectors: 'Telegram and Git: talk to an agent from a chat app, and give it a real repository to work in.',
  },
  conceptsDoc: {
    workspaces: 'The top-level boundary. A workspace isolates its own tasks, projects, files, memory pools, views and budget. Switch the active workspace from the top bar; most pages are scoped to it.',
    projects: 'Projects live inside a workspace and represent an actual deliverable or code repository — with repo settings, frontend/backend metadata and a group of related tasks.',
    tasks: 'A unit of work with a status, an assigned agent, execution logs, an activity history and a result. Tasks can be decomposed into sub-tasks and executed by an agent.',
    agents: 'Every agent is a folder of layered markdown — `instructions.md` (its role), `capabilities.md` (what it can do) and `usage.md` (when to call it) — plus a registry entry that holds the executable settings (model, provider, tools, memory binding).',
    agentsCallout: 'The three markdown layers are concatenated into the runtime system prompt. The model, provider and tools live in the registry, so editing a prompt never means touching JSON.',
    views: 'A view is a declarative, renderable presentation an agent chooses to produce instead of a wall of text — chart, graph, 3D scene, simulation, table, slides, document, equation or sandboxed HTML. Runs reference a view rather than embedding it, so the same object renders in Chat, in the gallery, and degrades to a one-line summary where it cannot be drawn.',
    scheduledJobs: 'A job is a future action — notify me, start an agent task, or trigger a flow — with an optional hourly / daily / weekly recurrence. Agents can schedule their own, and the results land in the notification inbox.',
  },
  featuresDoc: {
    lead: 'Agents Hub bundles everything needed to plan, run and supervise multi-agent work. Each card below opens the live feature; the Docs sidebar has a dedicated page per feature with a worked, interactive example.',
    tasksCard: 'Create, assign, decompose and execute tasks; inspect execution logs, activity and results.',
    planCard: 'Schedule notifications, agent tasks and flow triggers; read what fired in the inbox.',
  },
  chatDoc: {
    lead: 'Chat is the fastest way to put an agent to work — no need to start a node first. You talk to a selected agent (or a flow, or a team), and it can call tools, run sub-tasks, and read or write memory as it answers. Responses stream token by token.',
    modes: 'Three target modes per conversation — a single **agent**, a **flow**, or a **team** — picked from the switcher above the composer. The model comes from the top-bar picker.',
    viewInline: 'When an agent answers with a [view](/views) it renders in the bubble — chart, graph, 3D scene, slides — not as JSON.',
  },
  importingDoc: {
    lead: 'Not every agent should be rebuilt here. A code agent that already exists and is already tested — with its own prompts, its own dependencies and its own model access — can be imported as-is from its git repository. The hub clones it, checks whether it can run, and adds it to the agent list either way.',
    callout: 'Imported agents run **outside** this process. The hub never imports their Python, so their dependency tree stays theirs; prompts are forwarded over HTTP. Use [Agents → Import from Repo](/agents).',
    contractLead: 'One manifest file and one HTTP endpoint. That is the entire contract — everything else about the agent stays its own business.',
    streamingLead: 'A code agent can work for minutes. Declaring `runtime.stream_path` makes the hub stream the run instead of awaiting it, and the agent\'s output lands in the chat bubble token by token — the same rendering a built-in agent gets, because the frames are translated onto the same event vocabulary and pushed through the same emitter.',
    usageTip: 'The `usage` frame is what restores token and cost accounting. The model call happens in the agent\'s own process, so it is the only party that can know those numbers — when it reports them the hub credits the run exactly as it would a local call.',
    streamingWhen: 'Streaming is used only when both halves are present: the repository declared the endpoint *and* something on this side is listening (a chat run, or a parent agent that delegated to it). Otherwise the single POST runs — one round trip, and every imported agent must support it anyway. The `done` frame is authoritative; a stream that ends without one still yields the tokens it already sent.',
    aiderLead: '[Aider](https://github.com/Aider-AI/aider) is a well-known open-source code agent that edits files in a git repository and commits its own changes. Making it importable takes three files and no changes to aider itself. The complete example ships with this repository at `examples/imported-agents/aider-agenthub`.',
    adapterLead: '`server.py` is the whole integration: one HTTP call becomes one `aider --message` run.',
    unbufferedCallout: 'The one non-obvious detail is `PYTHONUNBUFFERED=1` in the child environment. Without it a Python child writing to a pipe buffers in 8 KB blocks, and the "stream" arrives as a couple of bursts at the end.',
    step0: 'Build and start the adapter: `docker build -f Dockerfile.agenthub -t aider-agenthub .` then run it on port 8410 with your model key and the repository to edit mounted at `/work`.',
    step1: 'Open [Agents](/agents) and press **Import from Repo**. Paste the repository URL and press **Check repository**.',
    step2: 'Read the readiness report. Anything missing is listed with the fix next to it — that is the same list the agent card will carry if you import now.',
    step3: 'Enter `http://localhost:8410` as the endpoint and press **Re-check**, then import. The agent appears in the list and can be used from Chat, tasks and flows like any other.',
    exampleTip: 'The example lives inside this repository, so it is not a git repo of its own. Run `examples/imported-agents/prepare_example_repo.sh` to get a clone-able copy in a temp directory, and paste the path it prints into the import dialog — no network needed.',
    giveUpLead: 'The process boundary that keeps a foreign agent\'s dependencies out of the hub also hides its internals, and three things follow from that:',
    giveUp0: '**Token and cost accounting depend on the agent.** LangChain callbacks observe nothing across a process boundary, so a remote that sends no `usage` frame leaves the cost columns at zero. One that does is accounted for normally.',
    giveUp1: '**No hub tools.** The imported agent uses its own tool layer; the tool list in its manifest is documentation, not a grant.',
    giveUp2: '**The hub cannot see inside a run.** Guards that work by intercepting an agent\'s own loop — tool-repetition limits, the context-window guard, the capability guard — do not apply to a remote agent. Its safety is its repository\'s business.',
  },
  skillsDoc: {
    runtime: 'Agents can also write their own skills at runtime with `create_skill`. Those show up here as "learned by an agent", ready to review and publish.',
    lead: 'A skill is a reusable procedure — "when to use this" plus ordered steps. An agent sees the names and descriptions of its skills in its system prompt, gets the matching steps injected when an instruction matches, and can pull the full text of any of them with `get_skill`.',
    install: '**Install** copies it into your workspace — it is never shared by reference, so the original\'s author cannot later rewrite what your agents read.',
  },
  webLogsDoc: {
    lead: 'Every `web_search` and `fetch_url` call an agent makes, recorded with the text it actually received. Two questions this answers:',
    refusals: 'Refusals are logged too: an SSRF block, a domain-policy denial, or search results withheld because their host is on the deny list.',
    callout: 'Flags are signals for review, not verdicts — a page discussing prompt injection gets flagged like one attempting it. Blocking is done elsewhere: the SSRF guard, the domain policy and the capability model. Turn recording off with `WEB_LOG_ENABLED=false`.',
  },
  viewsDoc: {
    lead: 'A **view** is a self-contained, declarative presentation an agent produces instead of a wall of text. One noun covers every form: chart, graph, 3D scene, simulation, math plot, process diagram, table, slides, document, equation, and sandboxed live HTML. Agents create them with the `create_view` tool, and a run then references the view rather than embedding it.',
    envelope: 'Because the presentation is declarative, the same object renders inline in Chat, opens full-screen from the [Views](/views) gallery, and degrades to its one-line `summary` where it cannot be drawn at all — Telegram, a plain-text log, a task result.',
    dataSeparate: 'Data is kept separate from presentation, so the same spec can be re-pointed at new data — which is what lets a flow, a replay or a scheduled run reuse a view. Views live with their workspace, at `<workspace>/.views/<view_id>/`.',
    visualizerTip: 'A built-in **Visualizer** agent ships with the hub already carrying the visualization toolset — the quickest way to see a view without wiring tools onto an agent of your own.',
    studioLead: '[Studio](/studio) binds a chat conversation to one live view. A live view is not written once: it is an ordered log of **ops** (`add` / `update` / `remove` / `clear` at a path such as `spec.nodes.n1`), and the document you see is the fold of that log. Collections are keyed maps rather than arrays, so every element is addressable and re-applying an op is idempotent.',
  },
  planDoc: {
    lead: '[Plan](/plan) is the scheduler: future actions, and the inbox of what they produced. A job is deliberately *not* a task — a Task record is only materialised when an `agent_task` job actually fires.',
    recurrence: 'Each job takes an absolute time or a relative delay, plus a recurrence of `none` / `hourly` / `daily` / `weekly`, and moves through scheduled → fired (or paused / cancelled / failed). The background scheduler\'s liveness is one of the things `GET /api/health` reports.',
    agentTools: 'The scheduling tools are bindable like any other: `schedule_notification`, `schedule_task`, `notify_user`, `list_scheduled`, `cancel_scheduled` and `update_scheduled`. An agent that finds work it cannot finish now can put it in the calendar instead of dropping it.',
    inboxTip: 'Anything a job or an agent sends you lands in the notification inbox — the bell in the top bar carries the unread count, and the Notifications tab on the Plan page is the full list.',
  },
  teamsDoc: {
    lead: 'A team is a bounded set of agents that know each other — up to twelve. Where a flow wires agents output-to-input and the Orchestrator routes a task to whichever agent looks suitable, a team is the middle case: a fixed, small cast, each member carrying a **manifest** of what it commits to doing on *this* team, with the charter and the full roster in every member\'s prompt — so agents address colleagues by name knowing what each one is for.',
    board: 'Members communicate over a shared message board: broadcasts by default, `@name` for a message aimed at one teammate. A run is *M members × R rounds* of agent calls, so it starts on a background thread and the page follows it live — reading the board afterwards shows exactly the same rows.',
    callout: 'Rounds multiply calls, so press **Estimate** before **Run** — it returns the upper-bound call count and projected spend for a full run. A team is attachable anywhere an agent is: to a task, or as the target of a chat conversation.',
  },
  loopsDoc: {
    lead: 'A flow is a DAG: it runs each node once and stops with whatever it produced. A [loop](/loops) wraps a flow and runs it again — feeding each pass the previous result and a reviewer\'s feedback — until an agent judges the work good enough.',
    records: 'Every run records *why* it stopped (criterion met, target score, max iterations, no improvement, cost ceiling, wall clock, stopped, flow failed, error) and keeps each iteration whole — its own flow run, output, score, verdict and feedback — so the page shows the trajectory (40 → 65 → 78 → 84) rather than only the final answer.',
  },
  evalsDoc: {
    lead: '[Evals](/evals) turns "did that prompt change help?" into a number. An eval set is a list of cases — an input plus what a good answer looks like — and a sweep runs every case against every provider/model combination you select, producing a score matrix.',
    sweepCallout: 'A sweep is real, slow, billable LLM calls — cases × models × graders. Press **Estimate** first; it projects the spend before anything runs.',
    cases: 'The cheapest source is production: any recorded run can be turned into a case in one click, so a bug you just watched happen becomes a regression test. The sibling of that is **Replay** — re-run a recorded run against a different model and diff the two outputs, with each side\'s tokens and cost. Replays are recorded on their own channel and excluded from cost, usage and budget aggregation.',
    injection: 'A separate harness ships in the repository for the security question rather than the quality one: it checks whether an agent obeys instructions embedded in fetched web content.',
  },
  yourOwnWorlds: 'Worlds you build yourself',
  playgroundDoc: {
    worlds: 'The two shipped environments are code; a world of your own is a description. [Worlds](/playground/worlds) lets you declare the **locations** characters move between, the **items** they hand over, the **entities** that stand there with state an action can change, the **world values** every scenario can start at a different level, the per-character **values** actions move between people, the **roles** that say who may do what to whom, and the **actions** themselves — arguments, the requirements the world checks before anything happens, and the effects it applies when they hold. Nothing is evaluated: a condition is a comparison against a value you declared, so a world stays a description of a world. Start from one of the ready-made ones and change it, then cast a scenario in it exactly as in a shipped environment. A world does not have to be typed in: the **World Builder** agent builds one from a sentence, and each world\'s page carries a build chat beside the form, so an edit can be asked for rather than filled in.',
    lead: 'The [Playground](/playground) drops several agents into a simulated world and lets them act, tick by tick. It answers a different question than Evals: not "is this answer right?" but "how does this agent behave among others, over time, under incentives?"',
    scenarios: 'A scenario is an environment plus a cast. Each **character** binds an agent to a name, a goal and — the interesting part — **private knowledge** only that agent has, which is what makes negotiation, bluffing and information asymmetry possible. Its **role** is the one thing it does not own: that is the kind of character the world declared, and it decides which actions the character may take and whom it may take them against. Cast somebody in no role — or misspell one — and they play the world\u2019s **generic role** instead: everything it leaves unreserved, nothing it reserves for a role. The world page spells out what that amounts to.',
    tickLog: 'A run is *N agents × T ticks* of LLM calls — minutes, not seconds — so it starts on a background thread and streams. Every tick is recorded with each agent\'s action and the state that followed, and every turn is also an ordinary run record with its own log, so one agent\'s work is inspectable from [Messages](/messages) like any other. Stop is immediate: the calls in flight are interrupted rather than allowed to finish the tick. The per-turn timeout is a **silence** timeout — a model that is still streaming is still working, so only one that has sent nothing for that long forfeits its turn.',
    activation: 'A scenario runs one of two ways. **Synchronous** wakes every agent every tick and resolves their actions simultaneously — predictable, and *N agents × T ticks* of model calls whether or not anyone had something to do. **Triggered** wakes an agent only when something reached it: a message, an interaction the world reports, its own `wake every` heartbeat, or an external poke sent to `POST /api/playground/runs/{id}/trigger` (there is a composer for it on the run page). A triggered world with an idle grace above zero stays open, waiting to be poked, instead of ending the moment it goes quiet.',
  },
  modelsDoc: {
    cached: 'Cached input is its own price because the provider bills it as its own line. An agent loop re-sends the whole conversation every step, and the provider serves all but the first copy of that prefix from its prompt cache at a fraction of the input rate. A model with no figure of its own is given a tenth of its input price, which is what the major providers charge. Each row also records whether its price was filled in automatically or edited by hand, and an edited price is never overwritten by a later discovery.',
    contextWindow: 'A model can also carry its context window — the number the chat\'s context meter draws against. Left at 0 the window is unknown, and the meter draws nothing rather than inventing a ceiling.',
    lead: '[Models](/models) is the single source of truth for *which* model runs. Settings holds credentials; the choice of model — and its price — lives here.',
    custom: 'Custom OpenAI-compatible backends declared in Settings appear here too, so a self-hosted endpoint is curated and priced exactly like a first-party provider.',
    usage: 'The second tab aggregates recorded runs into tokens, run counts and estimated spend per model — the same join of run records against catalog pricing that the [Costs](/costs) page slices by workspace, agent and project.',
    callout: 'A model with no price in the catalog contributes tokens but zero dollars. If a cost number looks impossibly low, check pricing for the models in that run first.',
  },
  costsDoc: {
    lead: 'Spend is derived entirely from recorded runs — the single source of truth for usage — joined against catalog pricing. [Costs](/costs) breaks it down by workspace, agent, model and project, over any date range.',
    budgets: 'Each workspace carries a budget: a **hard limit**, a **soft limit**, and a period of `total` / `daily` / `monthly` (UTC). The soft limit warns; the hard limit is checked at run launch and refuses to start a run that would cross it. A limit of `0` means unlimited, so the feature stays opt-in and an untouched workspace behaves exactly as before.',
    evalTip: 'Evaluation-channel runs (replays, and the judging calls inside an eval sweep) are tracked but excluded from budget aggregation, so measuring your agents never eats the budget your agents run on.',
  },
  containersDoc: {
    lead: '`AGENT_EXECUTION_MODE=local` (the default) runs agents as subprocesses on the host — simple, fast, and what most local work wants. `docker` runs each agent inside a managed container instead, which is what you want when an agent gets shell access or an untrusted repository.',
    callout: 'Compose already wires this up: the backend service gets the host Docker socket and a Docker CLI, and `HOST_PROJECT_ROOT` tells it which host path is mounted at `/app`, so the bind mounts it hands the daemon resolve correctly. What is off by default is the mode itself: set `AGENT_EXECUTION_MODE=docker`, restart the backend, then build the agent base image from this page. Mind what the socket grants, though: anything running in the backend container can drive the host daemon, so drop that mount from `docker-compose.yml` to forbid it outright.',
  },
  connectorsDoc: {
    lead: 'Two integrations reach outside the dashboard. Both are configured from [Settings](/settings) and both store their tokens write-only — you can replace or clear a token, never read it back.',
    telegram: 'Give the bot token, enable it, and a poller starts. Each chat is **bound** to an agent, so a message in that chat is a chat turn with that agent — the same run record, the same tools, the same memory as the web UI. Views degrade to their summary text there, since Telegram cannot draw them. The Settings tab shows poller status, last poll, last error, and the list of bindings.',
    git: 'A GitHub or GitLab token (GitLab also takes a base URL for self-hosted instances) lets the hub list the repositories you can reach and attach them to projects, so an agent gets a real codebase to work in rather than a bare directory.',
  },
  memoryDoc: {
    lead: 'Agents have five complementary memory layers bound to a pool (usually the workspace): shared (notes, structured slots, journal), episodic (events), procedural (skills), a knowledge graph, and RAG over indexed documents. The vector store and embedding model behind the RAG layer are chosen in [Settings → RAG & Vectors](/settings/rag).',
    episodic: 'Episodic memory captures discrete events; procedural memory stores reusable skills.',
  },
  nodesDoc: {
    title: 'Nodes & Sessions',
    lead: 'A **node** is a long-running agent process; a **session** (run) captures one execution of a task, chat turn or flow, with its logs and metadata.',
    sessions: '[Sessions](/sessions) groups runs by the conversation or task they belong to; [Messages](/messages) is the flat run log — every recorded execution with its prompt, output, tool calls, token usage and cost. Open any run to replay it against a different model and diff the two answers.',
  },
  installDoc: {
    lead: 'One command installs everything and puts `ah` on your PATH. Everything below is that command taken apart, for the cases where you want a different shape. The same page ships as `docs/installation.md` in the repository.',
    prereqTitle: 'Prerequisites',
    prereqPython: '**Python 3.10+** (3.11 recommended), for the backend, the CLI and the agent runners.',
    prereqNode: '**Node.js 22+ and npm**, for the dashboard.',
    prereqDocker: '**Docker and Compose**, only for the containerized stack or Docker agent execution.',
    prereqKey: '**A provider key**: OpenAI, Anthropic, Google, or a local Ollama / LM Studio endpoint.',
    installerTitle: 'The installer',
    installerBody: 'It creates `.venv`, installs the service and the `ah` command editable, copies `.env.example` to `.env` if you have none, installs the dashboard\'s npm packages, and writes the shell integration into your startup file. Re-running is safe: an existing `.env` is never overwritten, and the shell block is rewritten in place rather than appended again.',
    installerFlags: 'Flags: `--cli-only` (client only, for use with `AGENTS_HUB_URL`), `--with-rag` (adds the RAG extras, which pull in torch), `--no-venv`, `--no-shell`, `--venv PATH`, `--python PATH`. Then, in a new terminal:',
    byHandTitle: 'By hand',
    byHandBody: '`pip install -e .` alone installs only the terminal client and its three dependencies; the extras read the requirement files in the repository, so `[backend]`, `[agents]` and `[rag]` stay in step with them. Editable is deliberate: the command follows the checkout, `git pull` included, instead of freezing a copy. The two servers also start directly:',
    byHandNoInstall: 'With nothing installed at all, `python -m cli` from the checkout is the same program as `ah`.',
    dockerTitle: 'With Docker',
    dockerBody: 'Backend on `:8000`, dashboard on `:8080`. `.env` is optional here: the stack comes up without provider keys and you add them from [Settings](/settings). `BACKEND_PORT` and `WEB_PORT` move the published ports, and the dashboard follows them because it talks to its own origin. The dashboard is the built bundle behind nginx, which also proxies `/api` and balances it across the backends behind it, so running more of them is one flag:',
    configTitle: 'Configuration',
    configBody: '`.env` is read on startup, and `.env.example` lists every variable with its default. Most of it is editable from the dashboard afterwards, and the split is worth learning once: [Settings](/settings) holds credentials, while [Models](/models) holds the catalog, meaning which models are offered, what each costs, and the default per provider.',
    verifyTitle: 'Checking it worked',
    verifyBody: 'Then open the dashboard, pick an agent in [Chat](/chat) and send something. A reply means the provider key, the model catalog and the run pipeline all work. If the run fails instead, [Health](/health) says which part did.',
    stateTitle: 'Where state lives',
    stateBody: 'Everything the service writes goes under `.agents_hub/` in the checkout: the SQLite database, the agent registry, the model catalog, workspace folders, memory pools, run logs and generated views. Deleting that folder resets the installation, and it is also the folder to back up. Agent prompts are the exception: they live in `agents/definitions/<id>/` in the repository, because they are source rather than state.',
    upgradeTitle: 'Upgrading',
    upgradeBody: 'An editable install needs no reinstall for code changes. Schema changes apply themselves: the first connection in any process ensures the schema, so the backend or a CLI command may equally be the one to run them.',
    troubleCallout: 'If it will not come up: the backend exiting immediately usually means the extras are missing; a dashboard that cannot reach the API is a port or CORS problem; runs failing the moment they start are credentials or a model that is not enabled. [Troubleshooting](/docs/faq) has the rest.',
  },
  cliGuide: {
    lead: '`ah` drives the service from a terminal. Everything the dashboard does to agents, tasks, workspaces, projects and nodes has a command here, and the command **is** the application rather than a client of it.',
    reachTitle: 'Two ways it reaches the service',
    reachDirect: '**Direct**, the default. The CLI imports the service and calls its functions in this process: no server has to be running, nothing is serialized, nothing crosses a socket. It needs the repository, its dependencies and the state directory reachable where you run it.',
    reachHttp: '**Over REST.** Set `AGENTS_HUB_URL` and the same commands go to a running backend instead, which is the case direct calls cannot serve: a service inside a container, most often. `ah config` prints which one is in use. Each invocation is a fresh process, so expect about a second and a half of import and bootstrap before anything happens.',
    getTitle: 'Getting the command',
    getBody: 'The installer puts `ah` on your PATH, along with `agents-hub`, the same program under its long name. See [Installation](/docs/installation).',
    commandsTitle: 'Everyday commands',
    commandsBody: 'Listings print an 8-character ID prefix, and every command that takes an ID accepts that prefix back. A call that runs an agent waits as long as the model takes, up to `AGENTS_HUB_AGENT_TIMEOUT` seconds (default 900, `0` for no limit). A write from the CLI reaches an open dashboard the way a write from an agent subprocess does, so tabs refetch instead of showing stale rows.',
    upTitle: 'Both servers, one command',
    upBody: 'Production is the default: no reloader, and the built bundle served with `/api` forwarded to the API. The bundle is rebuilt whenever its sources are newer than `dist/index.html`. Output from both is interleaved, `Ctrl-C` stops both, and if one exits on its own the other is stopped too. There is no `--workers`: the backend starts singletons of its own (the scheduler, the run watchdog, the Telegram poller) and a second worker would run a second copy of each.',
    dirsTitle: 'Working in your own directories',
    dirsBody: 'A workspace normally lives under the state directory, but it can instead point at a directory you already have. Nothing is copied: agents read and write the real files in place, and a git checkout keeps its history, so `git-status` and `git-pull` work on it immediately. An attached workspace or project is named after its folder. Detaching only drops the link: the directory is never deleted from here.',
    selectTitle: 'Selecting a workspace',
    selectBody: 'A process cannot change its parent shell\'s environment, so the selection is stored rather than exported, and every later command inherits it. Precedence is narrowest first: `--workspace` beats `AGENTS_HUB_WORKSPACE`, which beats the working directory, which beats the stored selection in `~/.config/agents-hub/cli.json`.',
    selectCwd: '**The working directory selects on its own.** An attached workspace is the directory it links to, so standing in it already names a workspace, and standing in an attached project names the project too. It is read from the links under the state root, so it costs no call and simply finds nothing when this machine\'s paths are not the ones the service resolves. `AGENTS_HUB_AUTO_WORKSPACE=0` turns it off.',
    shellTitle: 'The shell integration',
    shellBody: '`conda activate` is a shell function rather than a program, for the same reason: only the shell can change its own environment. `shell-init` prints that kind of function, picks the startup file from the shell, writes between markers, and rewrites that region on a re-run instead of stacking another copy. `shell-init --uninstall` takes it back out, `shell-init [shell]` prints the block to place yourself, and `--name hub` picks a different function name. Afterwards the commands that change the selection export it into the current shell, so two shells activated differently work in two workspaces at once.',
    remoteTitle: 'Against a containerized backend',
    remoteBody: 'What changes is that **paths are then resolved by the backend**, so a host path means nothing inside the container. Bind mount one parent directory into the backend service and pass the in-container path. A running container cannot gain new mounts, so mount the parent rather than each project; an unmounted path is refused rather than silently attached.',
    tip: 'The CLI lives in the `cli` package in the repository: `cli/main.py` is the application and every command, `cli/backend.py` is the direct-or-REST layer underneath it. The full reference also ships as `docs/cli.md`.',
  },
  apiDoc: {
    cliPointer: 'Driving all of this from a terminal instead is the [CLI](/docs/cli) chapter.',
  },
};
