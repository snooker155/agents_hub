# Agent runtime dependencies
# Pinned with lower bounds that match the host environment.
# create_tool_calling_agent requires langchain>=0.1.0; >=0.3 keeps parity with host.

langchain>=0.3.0,<1.0.0
langchain-openai>=0.3.0,<1.0.0
langchain-anthropic>=0.3.0,<1.0.0
langchain-google-genai>=2.0.0,<3.0.0
langchain-community>=0.3.0,<1.0.0
langchain-ollama>=0.3.0,<1.0.0
openai>=1.0.0
anthropic>=0.84.0
google-generativeai>=0.8.0
fastapi>=0.100.0
uvicorn>=0.23.0
filelock>=3.12.0
pyyaml>=6.0
# Cron expressions for the plan scheduler's cron recurrence.
croniter>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
# langfuse 3 reworked the client API (OTel-based); capped below it until the
# integration is ported.
langfuse>=2.0.0,<3.0.0
tiktoken>=0.7.0
httpx>=0.27.0
# HTML -> text for the fetch_url tool. tools/web.py degrades to a regex
# stripper without it, so this is a quality dependency, not a hard one.
beautifulsoup4>=4.12.0
requests>=2.31.0
pypdf>=4.0.0
