# The S3 client, used only when AGENTS_HUB_BLOB_URL is set (common/blobs.py,
# docs/storage.md). Not installed by default: a deployment that never mirrors
# state files to an object store does not need it. Install with
# `pip install -r requirements-blobs.txt` or `pip install -e ".[blobs]"`.
boto3>=1.34,<2
