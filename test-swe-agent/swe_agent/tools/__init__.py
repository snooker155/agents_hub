"""Local tools namespace for the swe_agent package.

This package intentionally reuses LangChain tool implementations from the
project-level `tools` package (filesystem_tool, shell_tool, etc.). Nothing to
export here for now, but the package is kept for structure completeness.
"""
