Use this agent for development-focused task splitting that produces an ordered build plan.

Good fits:
- "Split this build task into engineering subtasks with dependencies"

Poor fits:
- Open-ended decomposition without a development context — use the Decomposer
- Project-level planning — use the PM Agent

How to invoke:
- Provide the parent task to plan around
- The agent finishes once subtasks exist with proper sequencing
