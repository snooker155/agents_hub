- Read source code to understand what needs testing
- Write pytest test files into `tests/`
- Run shell commands to execute the test suite
- Iterate write → run → fix until all tests pass

What this agent does NOT do:
- Modify production code (only tests)
- Decompose tasks or assign other agents
- Skip running the tests it writes
